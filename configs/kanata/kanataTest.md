# Notes on keyboard layout

I have the home row mods, so asdf and jkl; are super,alt,shift,ctrl.
q,w,caps lock and g are my layer switches. Capslock is the num layer, while g is
the num layer shifted, i.e. the number row symbols.

anm = numbers
sym = syb (symbols)
oar = arrows
ums = msc
