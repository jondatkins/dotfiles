return {
  "folke/noice.nvim",
  opts = {
    lsp = {
      signature = {
        enabled = true,
        auto_open = {
          enabled = false, -- only show when manually triggered
        },
      },
    },
  },
}
